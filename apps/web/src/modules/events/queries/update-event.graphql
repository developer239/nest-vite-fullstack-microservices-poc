mutation UpdateEvent($id: ID!, $input: UpdateEventInput!) {
  updateEvent(id: $id, input: $input) {
      id
      title
      description
      startsAt
      capacity
      owner {
          id
          firstName
          lastName
      }
      attendees {
          id
          firstName
          lastName
      }
  }
}
